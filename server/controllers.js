// const db = require('../database/model.js');

module.exports = {
  users: {
    post: (req, res) => {
      console.log('in post');

      // const { text } = req.body;
      // db.todos.post(text, (err, results) => {
      //   if (err) {
      //     res.status(404).send(err)
      //   } else {
      //     res.status(201).send(results)
      //   }
      // })
    }

  }
}